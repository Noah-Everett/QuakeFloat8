\documentclass[11pt,letterpaper]{article}

% ── Packages ──
\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{mathtools}
\usepackage{enumitem}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{xcolor}
\usepackage{tcolorbox}
\usepackage{titlesec}
\usepackage{fancyhdr}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{tabularx}
\usepackage{array}
\usepackage{graphicx}

% ── Styling ──
\hypersetup{colorlinks=true, linkcolor=blue!60!black, citecolor=green!50!black, urlcolor=blue!70!black}
\pagestyle{fancy}
\fancyhf{}
\rhead{\small QuakeFloat8}
\lhead{\small April 2026}
\cfoot{\thepage}

\titleformat{\section}{\Large\bfseries\color{blue!60!black}}{}{0em}{}[\vspace{-0.5em}\rule{\textwidth}{0.4pt}]
\titleformat{\subsection}{\large\bfseries}{}{0em}{}
\titleformat{\subsubsection}{\normalsize\bfseries}{}{0em}{}

% ── Theorem Environments ──
\newtheorem{theorem}{Theorem}[section]
\newtheorem{corollary}[theorem]{Corollary}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{conjecture}[theorem]{Conjecture}
\theoremstyle{definition}
\newtheorem{definition}[theorem]{Definition}
\theoremstyle{remark}
\newtheorem*{remark}{Remark}

% ── Boxes ──
\newtcolorbox{keyresult}{colback=green!5!white, colframe=green!50!black,
  title={\textbf{Key Result}}, fonttitle=\bfseries}

% ── Commands ──
\newcommand{\NMSE}{\mathrm{NMSE}}
\newcommand{\MSRE}{\mathrm{MSRE}}
\newcommand{\MSE}{\mathrm{MSE}}
\newcommand{\SQNR}{\mathrm{SQNR}}
\newcommand{\E}{\mathbb{E}}
\newcommand{\R}{\mathbb{R}}
\DeclareMathOperator*{\argmin}{arg\,min}
\DeclareMathOperator*{\argmax}{arg\,max}

% ── Document ──
\begin{document}

\begin{center}
  {\LARGE\bfseries QuakeFloat8: A Provably Optimal Log-Domain 8-Bit Number Format} \\[12pt]
  {\large April 2026} \\[4pt]
  {\normalsize Noah Everett} \\[2pt]
  {\small Department of Physics, Harvard University}
\end{center}

\vspace{1em}

\begin{abstract}
QuakeFloat8 (QF8) is a block-scaled logarithmic 8-bit number format in which
multiplication reduces to 7-bit integer addition.
The format uses a 1-bit sign, a 7-bit \texttt{u3.4} fixed-point $\log_2$ code
per element, and a shared E8M0 block exponent per 32 elements.

We prove that QF8's log-uniform spacing is minimax-optimal: the
\emph{equalization property} guarantees that the mean squared relative error
(MSRE) equals $\varepsilon^2/12$ for \emph{any} source distribution, and no
other fixed-rate quantizer achieves lower worst-case normalized MSE
(Theorem~\ref{thm:minimax}).
This density-independence eliminates the need for per-signal calibration.

Gate-level estimates show the QF8 multiply path at ${\sim}66$~gates vs.\
${\sim}231$ for FP8 ($3.5\times$ cheaper), with the full MAC unit at
${\sim}650$~gates vs.\ ${\sim}1350$ for FP8 (${\sim}2\times$ cheaper).
All estimates are NAND2-equivalent; no RTL synthesis has been performed.

Empirical validation across 1{,}481 weight tensors from 15 pretrained models
confirms the equalization property: QF8's SQNR is $38.1 \pm 0.10$~dB ---
a $+6.6$~dB advantage over FP8 E4M3 at identical storage cost.
Weight-only post-training quantization from 124M to 7B parameters shows
degradation ${\leq}0.03\%$ on WikiText-2 perplexity.
The format's theoretical guarantees --- distribution-independent quantization
error, multiplication via integer addition, and low hardware cost --- apply
broadly to any domain requiring efficient fixed-point arithmetic with controlled
relative error, including telecommunications, edge computing, and scientific
computing.
\end{abstract}

\tableofcontents
\newpage


%% ═══════════════════════════════════════════════════════
\section{Introduction}
\label{sec:intro}
%% ═══════════════════════════════════════════════════════

\subsection{The 8-Bit Frontier}

The push toward low-precision arithmetic has been remarkably successful across
multiple domains.
In machine learning, FP16 mixed-precision training~\cite{micikevicius2018} is standard practice,
BFloat16 is ubiquitous, and 8-bit formats (INT8, FP8 E4M3/E5M2~\cite{micikevicius2022}) are
deployed in production hardware (NVIDIA H100, AMD MI300, Google TPU).
The OCP Microscaling (MX) specification~\cite{rouhani2023} codifies 8~bits per element with
block scaling as sufficient for both training and inference of large language
models.
In telecommunications, logarithmic companding ($\mu$-law and A-law) has been
the foundation of PCM quantization since the 1960s~\cite{smith1957}, providing approximately
constant signal-to-noise ratio across signal levels.
In edge computing and IoT, the energy cost of arithmetic --- particularly
multiplication, which dominates at $>10\times$ the cost of addition~\cite{horowitz2014} ---
makes low-precision formats with cheap multiply operations attractive for
on-device inference.
In scientific computing, reduced-precision formats accelerate simulations in
climate modeling, molecular dynamics, and computational physics, where
\emph{relative} accuracy across many orders of magnitude matters more than
absolute precision near zero.

Yet a gap remains between the precision delivered by current 8-bit formats and
what is theoretically achievable.
FP8 E4M3 provides 8~representable levels per octave (factor of~2).
Is this optimal? Can we do better at the same bit budget?

\subsection{The Quake Insight}

In 1999, the Quake III source code revealed a remarkable hack: the integer
bit pattern of an IEEE~754 float is approximately a linear function of
$\log_2(x)$.
Reinterpreting float bits as integers, and vice versa, allows cheap approximate
logarithmic arithmetic.

QF8 makes this accident intentional.
By storing values as fixed-point $\log_2$ codes, multiplication becomes integer
addition --- replacing an $O(n^2)$-gate multiplier with an $O(n)$-gate adder.
The format is paired with MX-style block scaling (E8M0 shared exponent per 32
elements) to achieve full FP32 dynamic range.

\subsection{Prior Work}

\textbf{Log-domain ML arithmetic.}
The closest prior work is Johnson's (2018) ELMA format~\cite{johnson2018}, which demonstrated
that log-domain multiply with Kulisch accumulation is viable for 8-bit deep
learning --- QF8's core multiply-as-addition mechanism is Johnson's.
Johnson used a posit-style tapered encoding~\cite{gustafson2017}, which provides variable
precision across the range; QF8 replaces this with a simpler fixed-width
\texttt{u3.4} code.
Miyashita et al.~\cite{miyashita2016} explored logarithmic data representation for CNNs,
and the Logarithmic Number System (LNS) has been studied since the
1970s~\cite{kingsbury1971, swartzlander1975}.

\textbf{Block scaling.}
The OCP Microscaling (MX) specification~\cite{rouhani2023} established E8M0 block scaling as a
practical way to decouple dynamic range from per-element precision.
QF8 adopts this directly: the difference is the per-element encoding
(log code vs.\ IEEE-like floating point).

\textbf{Distribution-aware quantization.}
NF4~\cite{dettmers2023} (QLoRA) places levels at Gaussian quantiles, achieving optimal
quantization for a specific distribution.
QF8's log-uniform spacing takes the opposite approach: minimax optimality
across \emph{all} distributions (Section~\ref{sec:equalization}).

\textbf{Classical quantization theory.}
Bennett~\cite{bennett1948} established the high-resolution framework for quantization
distortion.
Panter and Dite~\cite{panter1951} derived the MSE-optimal compander (the one-third-power
rule), which depends critically on the source distribution.
Smith~\cite{smith1957} observed that logarithmic companding achieves approximately
amplitude-independent SNR for PCM telephony, but his result concerns signal
level, not source distribution, and his distortion formula remains
distribution-dependent (e.g., $0.707$ for exponential, $0.798$ for Gaussian
inputs).
Most directly related, Sun and Goyal~\cite{sun2011} proved that logarithmic companding is
\emph{variable-rate} optimal for MSRE but showed it is \emph{not} generally
optimal for fixed-rate quantization.
Theorem~\ref{thm:minimax} provides the complementary fixed-rate minimax result:
the equalization property makes log-uniform quantization uniquely
density-independent, resolving the fixed-rate question.
The standard references~\cite{gray1998, gersho1992} provide comprehensive treatments of
quantization and companding theory.

\subsection{Contributions}

\begin{enumerate}[leftmargin=2em]
  \item \textbf{The equalization property and minimax optimality.}
    We prove that log-uniform quantization achieves
    $\MSRE = \varepsilon^2/12$ for \emph{any} source density
    (Lemma~\ref{lem:equalization}), because the $x^2$ terms in the NMSE
    numerator and denominator cancel exactly.
    This density-independence implies that log-uniform quantization is the
    unique fixed-rate minimax-optimal quantizer for NMSE over all source
    distributions (Theorem~\ref{thm:minimax}), complementing Sun and
    Goyal's~\cite{sun2011} variable-rate result with a fixed-rate minimax guarantee.
  \item \textbf{Format design.}
    QF8 combines a \texttt{u3.4} fixed-point log code (16 levels per octave)
    with E8M0 block scaling, achieving $2\times$ the precision per octave of
    FP8 E4M3 at the same effective storage cost (8.25 bits/element).
  \item \textbf{Hardware analysis.}
    Gate-level estimates (NAND2-equivalent, not synthesized) show the QF8
    multiply path at ${\sim}66$~gates vs.\ ${\sim}231$ for FP8
    ($3.5\times$ cheaper).
    The full MAC unit is estimated at ${\sim}650$ gates vs.\ ${\sim}1350$
    for FP8 (${\sim}2\times$ cheaper), achieving near-INT8 area while providing
    floating-point dynamic range.
    The $3.5\times$ multiply-path advantage is partially offset by the
    accumulation overhead (LUT decode + barrel shifter + Kulisch register),
    yielding the ${\sim}2\times$ net advantage for the complete MAC.
  \item \textbf{Empirical validation.}
    The equalization property is confirmed across 1{,}481 weight tensors from
    15 models and 5~architectures: QF8's SQNR is $38.1 \pm 0.10$~dB regardless
    of model or weight distribution.
    Weight-only PTQ from GPT-2 (124M) to Mistral-7B shows degradation
    ${\leq}0.03\%$ at 7B scale, vs.\ MXFP8's $+0.19$--$0.28\%$.
    W8A8 quantization exposes a limitation: QF8's log-uniform spacing is
    suboptimal for activations concentrated near zero.
\end{enumerate}


%% ═══════════════════════════════════════════════════════
\section{The QF8 Format}
\label{sec:format}
%% ═══════════════════════════════════════════════════════

\subsection{Encoding Scheme}

\begin{definition}[QF8 Encoding]
\label{def:qf8-encoding}
A QF8-encoded block of $k = 32$ values consists of:
\begin{itemize}[leftmargin=2em]
  \item \textbf{Shared block scale} (8 bits): An E8M0 power-of-2 exponent
    $s \in \{2^{-127}, \ldots, 2^{127}\}$.
  \item \textbf{Per-element code} (8 bits each): 1~sign bit
    $\sigma \in \{0,1\}$ and a 7-bit unsigned \texttt{u3.4} fixed-point code
    $c \in \{0, 1, \ldots, 127\}$.
\end{itemize}
The reconstruction formula is:
\[
  \hat{x} = (-1)^\sigma \cdot s \cdot 2^{(c - 64)/16}, \qquad c \neq 0
\]
with $c = 0$ encoding exact zero.
The effective storage cost is $8 + 8/32 = 8.25$~bits per element.
\end{definition}

\subsection{Logarithmic Spacing}

The \texttt{u3.4} code provides 3~integer bits and 4~fractional bits of
$\log_2|x/s|$, spanning 8~octaves with \textbf{16 representable levels per
octave}.
This is the \emph{log-uniform quantizer}: the ratio between consecutive
representable values is constant at $r = 2^{1/16} \approx 1.0443$, giving a
maximum relative quantization error of
\[
  \frac{r - 1}{2} \approx 2^{1/16} - 1 \approx 4.43\%.
\]
Compare FP8 E4M3, which has 8~levels per octave (3-bit mantissa) and a maximum
relative error of $1/16 = 6.25\%$ within each binade.

\begin{remark}[Bit Allocation]
\label{rem:why-u34}
The 7-bit log code admits several integer/fractional splits.
The \texttt{u3.4} choice balances range and precision:
\begin{itemize}[leftmargin=2em]
  \item \textbf{Per-element range:} 3~integer bits give $2^3 = 8$ octaves.
    With E8M0 block scaling absorbing inter-block variation, the per-element
    code only needs to cover \emph{within-block} spread.
    For typical signal blocks of size 32 (e.g., ML weight tensors, audio
    frames), empirical within-block spread is 3--5 octaves; 8~octaves provides
    comfortable margin.
  \item \textbf{Precision:} 4~fractional bits give 16~levels per octave
    ($\MSRE = 1.56 \times 10^{-4}$).
    The alternative \texttt{u4.3} (16~octaves, 8~levels/octave) halves
    precision for already-sufficient range.
    The alternative \texttt{u2.5} (4~octaves, 32~levels/octave) risks overflow
    for blocks with $>4$ octave spread.
  \item \textbf{Wide-range variant:} For signals requiring wider dynamic range
    (e.g., gradients in ML training), a \texttt{u4.3} variant
    (16~octaves, 8~levels/octave) is the natural analog of FP8's E5M2
    (Section~\ref{sec:future}).
\end{itemize}
\end{remark}

\subsection{Multiplication as Integer Addition}

For two QF8 values with log codes $c_a, c_b$:
\begin{align}
  \text{sign}_{\text{product}} &= \sigma_a \oplus \sigma_b
    \label{eq:sign-xor} \\
  \text{log-code}_{\text{product}} &= c_a + c_b \label{eq:log-add}
\end{align}
The product's log code is a simple 7-bit integer addition.
In hardware, this replaces the $4 \times 4$ mantissa multiplier needed for FP8
E4M3 with a 7-bit carry-lookahead adder --- an estimated ${\sim}3.5\times$
reduction in multiply-path gate count (66 vs.\ 231 gates).
The full MAC unit including accumulation is estimated at ${\sim}2\times$ cheaper
(650 vs.\ 1350 gates; see Section~\ref{sec:hardware}).
The difference between the $3.5\times$ multiply-path advantage and the
${\sim}2\times$ full-MAC advantage arises from the accumulation stage:
QF8 requires a lookup table and barrel shifter to convert products back to
linear domain before accumulation (Section~\ref{subsec:accumulation}),
partially offsetting the multiply-path savings.

\subsection{Accumulation Strategy}
\label{subsec:accumulation}

The classical weakness of log-domain arithmetic is that \emph{addition} in the
original domain is expensive.
QF8 adopts the \textbf{Kulisch accumulation} strategy~\cite{johnson2018}:
\begin{enumerate}[leftmargin=2em]
  \item Multiply in log domain (integer addition of codes).
  \item Convert each product to linear domain via a $2^4 = 16$-entry lookup
    table.
  \item Accumulate in a wide fixed-point register (32--48 bits).
\end{enumerate}
This hybrid log-multiply / linear-accumulate approach gets cheap multiplies
\emph{and} exact (or near-exact) accumulation.

\subsection{BMD Realizability}

\begin{definition}[Bit-Manipulation Decodable (BMD)]
\label{def:bmd}
A decoder $\hat{D}: \{0,1\}^B \to \R$ is \emph{bit-manipulation decodable}
if it can be expressed as a fixed-length straight-line program over
$\{+, -, \times, \gg, \ll, \mathbin{\&}, |, \oplus\}$ on machine-word
integers, with the output reinterpreted as a float via type punning.
\end{definition}

\begin{proposition}[BMD Realizability of QF8]
\label{prop:bmd}
The log-uniform decode function $\hat{D}(n) = a \cdot r^n$ admits three
BMD implementations:
\begin{enumerate}[leftmargin=2em]
  \item \textbf{Lookup table:} $O(N)$ storage, $O(1)$ time.
  \item \textbf{Reinterpret cast (Quake trick):} $I = \alpha n + \beta$, then
    float reinterpret.
    Cost: 1~multiply + 1~add + type pun $= 3$~ops.
  \item \textbf{Quadratic approximation:}
    $2^{e+f} = 2^e \cdot (1 + 0.6602f + 0.3398f^2)$.
    Cost: 2~multiplies + 2~adds + 1~shift $= 5$~ops.
    Maximum relative error: $0.27\%$.
\end{enumerate}
\end{proposition}

\begin{proof}
Each construction can be verified by direct calculation.
The LUT is immediate.
The reinterpret cast follows from the observation that an IEEE~754 float's
bit pattern $I = 2^{23}(e + 127) + m$ is affine in $\log_2(x)$.
The quadratic approximation is a minimax-optimal polynomial fit to
$2^f$ on $[0,1)$.
\end{proof}

\subsection{Format Comparison}

\begin{table}[ht]
\centering
\caption{Comparison of 8-bit and related number formats.
  All block-scaled formats use $k=32$ elements per block.
  Gate counts are NAND2-equivalent estimates.}
\label{tab:format-comparison}
\begin{tabular}{@{}lccccc@{}}
\toprule
Feature & INT8 & FP8 E4M3 & FP8 E5M2 & BFloat16 & \textbf{QF8} \\
\midrule
Per-element bits     & 8     & 8     & 8     & 16    & 8 \\
Block scaling        & sym   & E8M0  & E8M0  & ---   & E8M0 \\
Eff.\ bits/element   & 8.25  & 8.25  & 8.25  & 16    & 8.25 \\
Levels per octave    & varies & 8    & 4     & 128   & \textbf{16} \\
Multiply gates       & 400   & 231   & 231   & ${\sim}850$ & \textbf{66} \\
Full MAC gates       & 750   & 1{,}350 & 1{,}350 & 2{,}400 & \textbf{650} \\
SQNR $\mathcal{N}(0,1)$ & 44.6$^*$ & 31.5  & ${\sim}25$ & ${\sim}52$ & \textbf{38.1} \\
Calibration-free     & No    & No    & No    & ---   & \textbf{Yes}$^\dagger$ \\
\bottomrule
\multicolumn{6}{@{}l@{}}{\footnotesize $^*$INT8 optimizes MSE, not NMSE;
  SQNR is distribution-dependent.} \\
\multicolumn{6}{@{}l@{}}{\footnotesize $^\dagger$For weight quantization.
  Activation quantization requires calibration (Section~\ref{subsec:ptq}).}
\end{tabular}
\end{table}


%% ═══════════════════════════════════════════════════════
\section{Hardware Cost Analysis}
\label{sec:hardware}
%% ═══════════════════════════════════════════════════════

This section presents gate-level hardware models anchored to published silicon
data from Horowitz (ISSCC 2014)~\cite{horowitz2014}, Johnson (2018 ELMA ASIC at 28nm)~\cite{johnson2018}, and
NVIDIA architecture whitepapers.
\textbf{All gate counts are NAND2-equivalent estimates.}
No RTL has been written and no synthesis has been performed.
The numbers should be treated as order-of-magnitude guidance, not as measured
silicon results.

\subsection{Reference Data}

\textbf{Horowitz ISSCC 2014} (45nm CMOS): 8-bit integer multiply = 0.2~pJ,
8-bit add = 0.03~pJ, 32-bit FP multiply = 3.7~pJ.

\textbf{Johnson ELMA synthesis} (28nm):
8-bit ELMA/38-bit Kulisch = $0.96\times$ power, $1.12\times$ area vs.\
INT8/32-bit MAC.
16-bit ELMA = $0.59\times$ power, $0.68\times$ area vs.\ FP16 FMA.

\subsection{QF8 Multiply Path}

The core QF8 advantage: multiplication reduces to a 7-bit integer addition plus
sign XOR.

\begin{table}[ht]
\centering
\caption{QF8 multiply-path gate count.}
\label{tab:qf8-multiply}
\begin{tabular}{@{}lcc@{}}
\toprule
Component & Gates & Area ($\mu\text{m}^2$, 7nm) \\
\midrule
Sign XOR                    & 1   & 0.04 \\
7-bit CLA adder             & 50  & 2.0  \\
Overflow/saturation detect  & 5   & 0.2  \\
Zero detection              & 10  & 0.4  \\
\midrule
\textbf{Total: multiply}    & \textbf{66} & \textbf{2.6} \\
\bottomrule
\end{tabular}
\end{table}

Compare: INT8 $8\times 8$ multiplier $= 400$ gates; FP8 $4\times 4$ mantissa
multiplier $+$ exponent logic $= 230$ gates; FP16 $11\times 11$ multiplier
$= 850$ gates; FP32 $24\times 24$ multiplier $= 3200$ gates.
The QF8 multiply path is \textbf{$3.5\times$ to $48\times$ cheaper} than
alternatives.

\subsection{QF8-Medium MAC Unit}

The recommended design point: 6-bit barrel shifter and 32-bit carry-save
Kulisch accumulator for intra-block accumulation, with block-pair results
reduced via FP32 arithmetic (matching MX-format accelerator practice).

\begin{table}[ht]
\centering
\caption{QF8-Medium MAC unit breakdown (7nm, 1 GHz).}
\label{tab:qf8-medium}
\begin{tabular}{@{}lcc@{}}
\toprule
Component & Gates & Area ($\mu\text{m}^2$) \\
\midrule
Multiply path (Table~\ref{tab:qf8-multiply})& 66   & 2.6  \\
exp2 LUT ($16 \times 12$-bit ROM)        & 100  & 4.0  \\
6-bit barrel shifter                     & 150  & 6.0  \\
32-bit carry-save Kulisch accumulator    & 300  & 12.0 \\
Control/mux                              & 34   & 1.4  \\
\midrule
\textbf{Total}                           & \textbf{650} & \textbf{26.0} \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Master Comparison}

\begin{table}[ht]
\centering
\caption{Per-MAC unit comparison at 7nm, 1 GHz.}
\label{tab:master-comparison}
\begin{tabular}{@{}lccccc@{}}
\toprule
Metric & INT8 & FP8 E4M3 & FP16 & FP32 & \textbf{QF8-Med} \\
\midrule
Gate count      & 750 & 1,350 & 2,400 & 5,950 & \textbf{650}  \\
Area ($\mu\text{m}^2$) & 30 & 54 & 96 & 238 & \textbf{26}   \\
Energy (pJ/MAC) & 0.040 & 0.070 & 0.130 & 0.350 & \textbf{0.033}\\
Pipeline stages & 2  & 3     & 3     & 3--4  & \textbf{2}    \\
Critical path (gates) & 24--30 & 35--45 & 38--48 & 55--70 & \textbf{20--26}\\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[ht]
\centering
\caption{QF8-Medium savings ratios vs.\ each baseline.}
\label{tab:savings-ratios}
\begin{tabular}{@{}lcccc@{}}
\toprule
Metric & vs FP32 & vs FP16 & vs FP8 & vs INT8 \\
\midrule
Area savings    & \textbf{89\%} & \textbf{73\%} & \textbf{52\%} & \textbf{13\%}  \\
Power savings   & \textbf{91\%} & \textbf{75\%} & \textbf{53\%} & \textbf{17\%}  \\
Latency improvement & 55--65\% & 35--45\% & 25--35\% & $\sim$5--10\% \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Systolic Array Scaling ($128 \times 128$)}

\begin{table}[ht]
\centering
\caption{128$\times$128 systolic array estimates at 7nm, 1~GHz.}
\label{tab:systolic}
\begin{tabular}{@{}lccccc@{}}
\toprule
Metric & INT8 & FP8 & FP16 & FP32 & \textbf{QF8-Med} \\
\midrule
Compute area (mm$^2$) & 0.49 & 0.88 & 1.57 & 3.90 & \textbf{0.43} \\
Total power (W)        & 0.68 & 1.19 & 2.21 & 5.93 & \textbf{0.56} \\
TOPS/W                 & 48.2 & 27.6 & 14.8 & 5.5  & \textbf{58.5} \\
TOPS/mm$^2$            & 66.9 & 37.3 & 20.9 & 8.4  & \textbf{76.2} \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Break-Even Analysis}

The log-domain advantage scales differently with bit-width because the
multiplier is $O(n^2)$ while the adder is $O(n)$:

\begin{table}[ht]
\centering
\caption{Log-domain break-even across bit widths.}
\label{tab:breakeven}
\begin{tabular}{@{}ccccl@{}}
\toprule
Bit-width & Mult saved (gates) & LUT overhead (gates) & Net & vs INT-$N$ \\
\midrule
4   & 38     & 120  & $-82$     & $1.5\times$ worse \\
6   & 130    & 180  & $-50$     & $1.2\times$ worse \\
\textbf{8}   & \textbf{364}    & \textbf{300}  & $+64$     & \textbf{$0.92\times$ (slight win)} \\
12  & 1,145  & 500  & $+645$    & $0.63\times$ \\
16  & 2,925  & 800  & $+2{,}125$& $0.40\times$ \\
\bottomrule
\end{tabular}
\end{table}

\textbf{The crossover is at ${\sim}7$--$8$~bits.}
Below 7~bits, the LUT and accumulator overhead exceeds multiplier savings.
Above 8~bits, log-domain wins decisively and the advantage grows quadratically.
This confirms Johnson's~\cite{johnson2018} finding that 16-bit ELMA achieves 41\% power
reduction over FP16.

\subsection{Summary}

\begin{itemize}[leftmargin=2em]
  \item \textbf{QF8 vs FP32/FP16:} Clear, large, robust win
    ($3$--$10\times$ savings).
  \item \textbf{QF8 vs FP8:} Clear win (${\sim}2\times$ area and power).
  \item \textbf{QF8 vs INT8:} Slight win ($0.87\times$ area,
    $0.83\times$ power).
    The carry-save Kulisch design tips the balance.
  \item \textbf{The real value:} $2\times$ precision-per-octave at
    INT8-equivalent hardware cost and half the cost of FP8.
\end{itemize}

\paragraph{System-level efficiency.}
The per-MAC energy advantages above apply to the compute datapath only.
Modern inference workloads are typically memory-bandwidth-bound, where
data-movement energy dominates arithmetic cost by two orders of
magnitude~\cite{horowitz2014}.
In this regime, QF8's primary efficiency contribution is indirect: at identical
8-bit bandwidth, QF8 delivers $+6.6$~dB higher SQNR than FP8 E4M3, potentially
enabling 8-bit deployment in settings that would otherwise require 16-bit
precision --- halving memory traffic and the associated energy cost.
Validating this claim requires demonstrating QF8 at 8~bits matching FP16 quality
on production-scale models, which we leave to future work.


%% ═══════════════════════════════════════════════════════
\section{The Equalization Property}
\label{sec:equalization}
%% ═══════════════════════════════════════════════════════

\subsection{Notation}

Let $X$ be a positive random variable with probability density function (pdf)
$f$ supported on $[a,b] \subset \R_{>0}$.
A $B$-bit scalar quantizer is a map $Q: [a,b] \to \hat{\mathcal{X}}$ with
$N = 2^B$ reconstruction points partitioning $[a,b]$ into cells
$\{S_k\}_{k=0}^{N-1}$ with codepoints $\{c_k\}$.

\begin{definition}[Distortion Measures]
\label{def:distortion}
Let $\delta_X = X - Q(X)$ and $\varepsilon_X = \delta_X / X$.
\begin{align}
  \MSE_X &= \E[\delta_X^2],
  & \NMSE_X &= \frac{\E[\delta_X^2]}{\E[X^2]}
    \label{eq:nmse-def} \\
  \MSRE_X &= \E[\varepsilon_X^2],
  & \SQNR &= 10\log_{10}(1/\NMSE) \text{ dB}
    \label{eq:sqnr-def}
\end{align}
\end{definition}

\begin{definition}[Log-Uniform Quantizer]
\label{def:log-uniform}
The log-uniform quantizer with $N$ levels over dynamic range $[a,b]$ has
cell boundaries $a \cdot r^k$ for $k = 0, \ldots, N$, where
$r = (b/a)^{1/N}$ and $R = \log_2(b/a)$ is the range in octaves.
With geometric midpoint codepoints $c_k = a \cdot r^{k+1/2}$, the relative
cell width is constant:
\begin{equation}
  \frac{w(x)}{x} = r - 1 = e^\varepsilon - 1 \approx \varepsilon,
  \qquad \varepsilon = \frac{R \ln 2}{N}
  \label{eq:rel-cell-width}
\end{equation}
\end{definition}

\subsection{Main Results}

\begin{lemma}[Equalization Property]
\label{lem:equalization}
For the log-uniform quantizer in the high-resolution regime ($N \gg 1$):
\[
  \MSRE = \frac{\varepsilon^2}{12} + O(\varepsilon^4)
\]
for \emph{any} pdf $f$ supported on $[a,b]$.
The MSRE is density-independent.
\end{lemma}

\begin{proof}
For the log-uniform quantizer, $w(x) = x \cdot \varepsilon + O(x\varepsilon^2)$.
Under Bennett's~\cite{bennett1948} high-resolution integral approximation:
\[
  \NMSE = \frac{\int_a^b \frac{w(x)^2}{12} f(x)\,dx}{\int_a^b x^2 f(x)\,dx}
  = \frac{\frac{\varepsilon^2}{12}\int_a^b x^2 f(x)\,dx}
         {\int_a^b x^2 f(x)\,dx}
  = \frac{\varepsilon^2}{12}
\]
The $x^2$ terms in the numerator and denominator cancel exactly.
Similarly, $\MSRE = \varepsilon^2/12$ for any density $f$.
\end{proof}

\begin{theorem}[Minimax NMSE Optimality of Log-Uniform Quantization]
\label{thm:minimax}
In the high-resolution regime ($N \gg 1$), among all $N$-level quantizers on
$[a,b]$, the log-uniform quantizer uniquely minimizes the worst-case NMSE
(and MSRE) over all densities:
\[
  Q^*_{\log} = \argmin_{Q \in \mathcal{Q}_N}\; \max_{f \in \mathcal{F}[a,b]}\;
  \NMSE(Q, f)
\]
The minimax value is:
\begin{equation}
  \boxed{\NMSE^* = \frac{\varepsilon^2}{12} + O(\varepsilon^4)
  = \frac{(R \ln 2)^2}{12 N^2}}
  \label{eq:minimax-nmse}
\end{equation}
For $B = 8$ bits, $R = 16$ octaves:
$\NMSE^* = 1.564 \times 10^{-4}$ ($\SQNR = 38.1$~dB).
\end{theorem}

\begin{proof}
By Lemma~\ref{lem:equalization}, the log-uniform quantizer achieves
$\NMSE = \varepsilon^2/12$ for every density $f$.
It remains to show that any non-log-uniform quantizer has strictly higher
worst-case NMSE.

\textbf{Step 1 (Coverage constraint).}
Define $\phi(x) = w(x)/x$ (relative cell width).
Any $N$-cell quantizer on $[a,b]$ satisfies:
\begin{equation}
  \int_a^b \frac{dx}{\phi(x)\,x} = N
  \label{eq:coverage}
\end{equation}
For the log-uniform quantizer, $\phi \equiv \varepsilon$, giving
$(1/\varepsilon)\ln(b/a) = N$.
Suppose for contradiction that $\phi$ is non-constant but
$\phi(x) \le \varepsilon$ everywhere.
Then $1/\phi(x) \ge 1/\varepsilon$ everywhere, with strict inequality on a set
of positive measure, so
\[
  N = \int_a^b \frac{dx}{\phi(x)\,x}
  > \int_a^b \frac{dx}{\varepsilon\,x}
  = \frac{\ln(b/a)}{\varepsilon} = N,
\]
a contradiction.
Therefore $\phi^* = \max_x \phi(x) > \varepsilon$ for any non-log-uniform
quantizer.

\textbf{Step 2 (Adversarial density).}
Since $\phi$ is piecewise constant on finitely many cells, the maximum
$\phi^*$ is achieved in the interior of some cell $S_{k^*}$.
For any $\eta > 0$ small enough that
$[x^* - \eta, x^* + \eta] \subset S_{k^*}$,
the uniform density on $[x^* - \eta, x^* + \eta]$ achieves:
\[
  \MSRE \geq \frac{(\phi^*)^2}{12} - g(\eta),
  \qquad g(\eta) \to 0 \text{ as } \eta \to 0
\]
Therefore
$\max_f \MSRE(Q, f) \geq (\phi^*)^2/12 > \varepsilon^2/12 = \MSRE^*$.

\textbf{Step 3 (Uniqueness).}
If $Q \neq Q_{\log}$, then $\phi$ is non-constant and Steps~1--2 give strict
inequality.
\end{proof}

\begin{corollary}[Minimax MSRE]
\label{cor:minimax-msre}
The same quantizer also minimizes worst-case MSRE.
The proof is identical, with $w(x)^2 f(x)/x^2$ replacing
$w(x)^2 f(x)/\E[X^2]$.
\end{corollary}

\begin{remark}[High-Resolution Regime]
The proof uses Bennett's high-resolution integral approximation, which is
asymptotically exact as $N \to \infty$.
For finite~$N$, the exact MSRE matches $\varepsilon^2/12$ to within $0.03\%$ at
$N = 256$ (Table~\ref{tab:msre-verify}), confirming that the approximation is
excellent for practical quantizer sizes.
However, we have not formally proved that the minimax \emph{ordering} holds for
small~$N$; the finite-$N$ evidence is numerical, not analytic.
See Appendix~\ref{sec:appendix-msre} for the exact MSRE derivation.
\end{remark}

\begin{remark}[Domain Restriction and Zero Handling]
\label{rem:zero-handling}
Theorem~\ref{thm:minimax} applies to densities on $[a,b] \subset \R_{>0}$,
excluding zero.
In practice, signals may contain exact zeros (e.g., ReLU outputs in ML, clipped
samples in audio).
QF8 handles this via the code $c = 0$ (exact zero) and block scaling
(smallest nonzero value as low as ${\sim}5.9 \times 10^{-39}$ with
$s = 2^{-127}$).
The minimax guarantee applies to the nonzero portion of the signal.
\end{remark}

\subsection{Relation to Classical Theory}
\label{subsec:classical}

The minimax framing is appropriate whenever the quantizer must handle
heterogeneous signal distributions without per-signal calibration --- a common
requirement in ML (where weight distributions vary across layers), in
telecommunications (where signal statistics change with channel conditions), and
in scientific computing (where physical quantities span many orders of
magnitude).
The equalization property guarantees that QF8's error bound holds in all these
settings.

This result complements and strengthens prior work.
Smith~\cite{smith1957} observed amplitude-independent SNR for log-PCM, but his
distortion formula remains distribution-dependent.
Sun and Goyal~\cite{sun2011} proved that log companding is variable-rate MSRE-optimal but
showed it is \emph{not} generally fixed-rate optimal.
The equalization property resolves this: log-uniform quantization \emph{is}
fixed-rate minimax-optimal, precisely because its MSRE is density-independent.
Log-uniform quantization also minimizes worst-case product error for
multiply-accumulate operations (Appendix~\ref{sec:appendix-product}).

\subsection{Numerical Verification}

The high-resolution approximation is confirmed by exact computation
(Appendix~\ref{sec:appendix-msre}) and by exhaustive evaluation over three test
densities (log-uniform, uniform, $f \propto x^2$) with $N = 256$, $R = 16$:
all give $\NMSE = 1.564 \times 10^{-4}$ within $< 0.1\%$ deviation.

\begin{table}[ht]
\centering
\caption{Numerical verification: exact MSRE vs.\ approximations.}
\label{tab:msre-verify}
\begin{tabular}{@{}ccccc@{}}
\toprule
$\varepsilon$ & Exact MSRE & $\varepsilon^2/12$ & $\varepsilon^2/2$ (wrong) &
  Exact / $(\varepsilon^2/12)$ \\
\midrule
0.01 & $8.333 \times 10^{-6}$  & $8.333 \times 10^{-6}$  &
  $5.000 \times 10^{-5}$  & 1.000 \\
0.05 & $2.083 \times 10^{-4}$  & $2.083 \times 10^{-4}$  &
  $1.250 \times 10^{-3}$  & 0.9999 \\
0.10 & $8.331 \times 10^{-4}$  & $8.333 \times 10^{-4}$  &
  $5.000 \times 10^{-3}$  & 0.9997 \\
0.20 & $3.329 \times 10^{-3}$  & $3.333 \times 10^{-3}$  &
  $2.000 \times 10^{-2}$  & 0.9989 \\
\bottomrule
\end{tabular}
\end{table}


%% ═══════════════════════════════════════════════════════
\section{Empirical Validation}
\label{sec:experiments}
%% ═══════════════════════════════════════════════════════

All experiments use identical block-scaled quantization ($k = 32$ elements) with
E8M0 shared exponents for all three formats (QF8, MXFP8 E4M3, INT8), ensuring
a fair comparison at the same effective storage cost (8.25 bits/element).
INT8 uses per-block symmetric scaling ($\text{amax}/127$).
All PTQ comparisons use simple quantize-dequantize round-trips without
calibration data or learned scaling --- the goal is to compare \emph{format-level}
accuracy at equal complexity, not to compete with calibration-based methods
(GPTQ, AWQ, QuIP\#) that achieve near-lossless results even at 4~bits by
optimizing the quantization grid per-layer.
Code and raw results are available at
\url{https://github.com/Noah-Everett/QuakeFloat8}.

\subsection{SQNR on Pretrained Weights}
\label{subsec:sqnr-pretrained}

We apply each format's block-scaled quantization round-trip to all 48 weight
tensors (2D+, excluding embeddings) of pretrained GPT-2 Small (124M parameters)
and measure SQNR per tensor.

\begin{table}[ht]
\centering
\caption{Per-layer SQNR on GPT-2 Small pretrained weights (48 tensors).}
\label{tab:sqnr-pretrained}
\begin{tabular}{@{}lccc@{}}
\toprule
Format & Mean SQNR & Min & Max \\
\midrule
QF8         & 38.0 dB & 37.8 dB & 38.1 dB \\
MXFP8 E4M3 & 31.5 dB & 31.4 dB & 31.6 dB \\
INT8        & 44.6 dB & 41.0 dB & 45.4 dB \\
\midrule
QF8 $-$ MXFP8 & $+6.5$ dB & $+6.2$ dB & $+6.5$ dB \\
\bottomrule
\end{tabular}
\end{table}

\begin{keyresult}
QF8 achieves $\SQNR = 38.1$~dB vs.\ FP8 E4M3's 31.5~dB ---
a \textbf{6.6~dB advantage}, corresponding to a ${\sim}4.6\times$ reduction in
quantization noise power.
This advantage is structural (16 vs.\ 8 levels per octave) and holds across all
tested distributions.

\medskip
\begin{tabular}{@{}lcc@{}}
\toprule
Format & SQNR on $\mathcal{N}(0,1)$ & Gap from QF8 \\
\midrule
QF8 (log-uniform, 256 levels) & 38.1 dB & --- \\
FP8 E4M3 (IEEE)               & 31.5 dB & 6.6 dB worse \\
\bottomrule
\end{tabular}
\end{keyresult}

The measured $+6.5$~dB advantage over MXFP8 is consistent with the structural
prediction of ${\sim}6.0$~dB from the levels-per-octave ratio
($16/8 = 2$, so $10\log_{10}(4) = 6.0$~dB), with the additional
${\sim}0.5$~dB attributable to FP8's within-binade non-uniformity
(Appendix~\ref{sec:appendix-separation}).
INT8 achieves the highest absolute SQNR (44.6~dB) because it minimizes MSE
rather than NMSE --- but its SQNR varies significantly across layers
(Section~\ref{subsec:equalization-experiment}).

\subsection{Cross-Model Equalization Experiment}
\label{subsec:equalization-experiment}

The equalization property (Lemma~\ref{lem:equalization}) predicts that QF8's
SQNR should be independent of the weight distribution.
To test this, we measure per-layer SQNR across 15 pretrained models spanning
5~architectures, 7~model families, and parameter counts from 60M to 1.3B:

\begin{table}[ht]
\centering
\caption{Cross-model SQNR stability (1{,}481 weight tensors from 15 models).}
\label{tab:equalization}
\begin{tabular}{@{}lcccc@{}}
\toprule
Format & Mean SQNR & Std Dev & Range & Kurtosis corr.\ ($r$) \\
\midrule
\textbf{QF8}   & \textbf{38.1 dB} & \textbf{0.10 dB} & 3.5 dB & $-0.08$ \\
MXFP8 E4M3     & 31.5 dB          & 0.13 dB          & 4.8 dB & $+0.06$ \\
INT8            & 45.1 dB          & 0.54 dB          & 8.9 dB & $-0.49$ \\
\bottomrule
\end{tabular}
\end{table}

Models tested: GPT-2 (Small, Medium, Large), OPT (125M, 350M, 1.3B),
Pythia (160M, 410M, 1B), BERT (Base, Large), RoBERTa-Base, DistilBERT,
T5 (Small, Base).
All 15 models are transformer variants; CNNs, RNNs, and diffusion models are
not represented.
The equalization property is mathematically distribution-independent, so it
should hold for any architecture, but the empirical confirmation here is limited
to transformer weight distributions.

\begin{figure}[ht]
\centering
\includegraphics[width=\textwidth]{fig_equalization_histogram.pdf}
\caption{SQNR distribution across all 1{,}481 weight tensors from 15 models.
QF8 (green) is a single spike at 38.1~dB ($\sigma = 0.10$~dB).
MXFP8 (red) clusters at 31.5~dB with $3\times$ more variance.
INT8 (blue) spreads over a ${\sim}9$~dB range, reflecting
distribution-dependent error.}
\label{fig:equalization-histogram}
\end{figure}

QF8's SQNR standard deviation across \emph{all} 1{,}481 tensors is 0.10~dB ---
$5\times$ lower than INT8's 0.54~dB.
Figure~\ref{fig:equalization-histogram} makes this visible: QF8 collapses to a
single spike, while INT8 is spread across a ${\sim}9$~dB range.

\begin{figure}[ht]
\centering
\includegraphics[width=\textwidth]{fig_equalization_kurtosis.pdf}
\caption{SQNR vs.\ weight distribution kurtosis (log scale).
QF8's correlation is $r = -0.08$ (flat line --- distribution-independent).
INT8's correlation is $r = -0.49$ (quality degrades for heavy-tailed
distributions).}
\label{fig:equalization-kurtosis}
\end{figure}

The kurtosis correlation (Figure~\ref{fig:equalization-kurtosis},
Table~\ref{tab:equalization}) quantifies dependence on weight distribution
shape: INT8's SQNR correlates with kurtosis ($r = -0.49$), meaning its quality
degrades for heavy-tailed distributions.
QF8's correlation is $r = -0.08$ --- effectively zero, confirming the
equalization property.

\textbf{Practical consequence:} QF8 weight quantization requires no per-layer
calibration.
Across all 1{,}481 tested tensors, the SQNR is consistently ${\sim}38$~dB
regardless of model architecture, training procedure, or weight distribution
shape.

\subsection{Post-Training Quantization on WikiText-2}
\label{subsec:ptq}

We evaluate PTQ perplexity on the WikiText-2 test set across three GPT-2 model
sizes.
For \textbf{weight-only} PTQ, all linear layer weights undergo a quantization
round-trip before inference.
For \textbf{W8A8}, activations (inputs to each \texttt{nn.Linear}) are
additionally quantized via dynamic block scaling at inference time using
forward pre-hooks.

\begin{table}[ht]
\centering
\caption{Weight-only PTQ perplexity on WikiText-2 (sliding window, stride 512).}
\label{tab:ptq-wonly}
\begin{tabular}{@{}llrr@{}}
\toprule
Model & Format & Perplexity & $\Delta$ vs FP32 \\
\midrule
GPT-2 Small  & FP32 (baseline)   & 24.997 & ---        \\
(124M)       & \textbf{QF8}      & \textbf{24.962} & $\mathbf{-0.14\%}$ \\
             & MXFP8 E4M3        & 25.033 & $+0.14\%$  \\
             & INT8              & 25.025 & $+0.11\%$  \\
\midrule
GPT-2 Medium & FP32 (baseline)   & 18.243 & ---        \\
(355M)       & \textbf{QF8}      & \textbf{18.241} & $\mathbf{-0.01\%}$ \\
             & MXFP8 E4M3        & 18.345 & $+0.56\%$  \\
             & INT8              & 18.246 & $+0.02\%$  \\
\midrule
GPT-2 Large  & FP32 (baseline)   & 15.436 & ---        \\
(774M)       & QF8               & 15.453 & $+0.11\%$  \\
             & MXFP8 E4M3        & 15.467 & $+0.20\%$  \\
             & INT8              & 15.433 & $-0.02\%$  \\
\bottomrule
\end{tabular}
\end{table}

\textbf{Weight-only PTQ (GPT-2).}
QF8 consistently matches or outperforms MXFP8 across all three GPT-2 sizes,
with degradation ${\leq}0.2\%$ everywhere.
At GPT-2 Medium (355M), the gap is most pronounced: QF8 degrades by only
$-0.01\%$ vs.\ MXFP8's $+0.56\%$.

\subsubsection{Scaling to 7B Parameters}
\label{subsubsec:scaling-7b}

To validate that QF8's advantage holds at the scale where quantization is most
relevant, we evaluate weight-only PTQ on three additional models up to 7B
parameters.

\begin{table}[ht]
\centering
\caption{Weight-only PTQ perplexity at 7B scale (WikiText-2, stride 512).}
\label{tab:ptq-7b}
\begin{tabular}{@{}llrr@{}}
\toprule
Model & Format & Perplexity & $\Delta$ vs FP16 \\
\midrule
Phi-2            & FP16 (baseline) & 8.646  & ---          \\
(2.7B)           & \textbf{QF8}    & \textbf{8.646} & $\mathbf{+0.01\%}$  \\
                 & MXFP8 E4M3     & 8.670  & $+0.28\%$    \\
                 & INT8           & 8.649  & $+0.04\%$    \\
\midrule
Mistral-7B       & FP16 (baseline) & 4.798  & ---          \\
(7B)             & \textbf{QF8}    & \textbf{4.798} & $\mathbf{+0.01\%}$  \\
                 & MXFP8 E4M3     & 4.809  & $+0.23\%$    \\
                 & INT8           & 4.798  & $+0.01\%$    \\
\midrule
Pythia-6.9B      & FP16 (baseline) & 8.351  & ---          \\
(6.9B)           & \textbf{QF8}    & \textbf{8.354} & $\mathbf{+0.03\%}$  \\
                 & MXFP8 E4M3     & 8.367  & $+0.19\%$    \\
                 & INT8           & 8.357  & $+0.06\%$    \\
\bottomrule
\end{tabular}
\end{table}

At 7B scale, QF8's weight-only degradation is ${\leq}0.03\%$ --- effectively
lossless.
MXFP8's degradation is $10$--$30\times$ larger ($+0.19\%$ to $+0.28\%$).
INT8 performs well on absolute perplexity but, as shown in
Section~\ref{subsec:equalization-experiment}, its SQNR varies significantly
across layers and models --- it offers no distribution-independent guarantee.

These results use single evaluation runs per configuration.
The perplexity differences at this scale ($0.002$--$0.02$ perplexity points
for QF8) are small enough that they should be interpreted as
``effectively lossless'' rather than as precise measurements.

\begin{table}[ht]
\centering
\caption{W8A8 PTQ perplexity on WikiText-2 (weights + activations quantized).}
\label{tab:ptq-w8a8}
\begin{tabular}{@{}llrr@{}}
\toprule
Model & Format & Perplexity & $\Delta$ vs FP32 \\
\midrule
GPT-2 Small  & FP32 (baseline)   & 24.997 & ---        \\
(124M)       & W8A8 QF8          & 25.429 & $+1.73\%$  \\
             & W8A8 MXFP8        & 25.053 & $+0.22\%$  \\
             & W8A8 INT8         & 26.310 & $+5.25\%$  \\
\midrule
GPT-2 Medium & FP32 (baseline)   & 18.243 & ---        \\
(355M)       & W8A8 QF8          & 18.504 & $+1.43\%$  \\
             & W8A8 MXFP8        & 18.361 & $+0.65\%$  \\
             & W8A8 INT8         & 18.594 & $+1.93\%$  \\
\midrule
GPT-2 Large  & FP32 (baseline)   & 15.436 & ---        \\
(774M)       & W8A8 QF8          & 15.453 & $+0.11\%$  \\
             & W8A8 MXFP8        & 15.472 & $+0.23\%$  \\
             & W8A8 INT8         & 15.433 & $-0.02\%$  \\
\bottomrule
\end{tabular}
\end{table}

\textbf{W8A8 PTQ: a known limitation.}
Quantizing activations exposes a structural weakness of log-uniform spacing.
At GPT-2 Small (124M), QF8's W8A8 penalty is $+1.73\%$ --- significantly worse
than MXFP8's $+0.22\%$ ($8\times$ larger).
At GPT-2 Medium (355M), QF8 is still $2.2\times$ worse than MXFP8 ($+1.43\%$
vs.\ $+0.65\%$).
The cause is fundamental: log-uniform spacing allocates precision uniformly
across magnitudes, but post-GELU and post-LayerNorm activations concentrate near
zero, where the log grid's resolution is coarsest.
At GPT-2 Large (774M), QF8's W8A8 penalty is $+0.11\%$
(comparable to MXFP8's $+0.23\%$), but with only three model sizes,
single-seed evaluation, and no error bars, this apparent scaling trend is not
statistically robust.

\textbf{Near-zero representation cliff.}
Within a single block, QF8's smallest nonzero magnitude is
$2^{(1-64)/16} \approx 0.065$ (code $c=1$).
Values jump discontinuously from exact zero ($c=0$) to $0.065$.
IEEE formats avoid this via denormals, but QF8's fixed-point log encoding has
no subnormal region.
In practice, E8M0 block scaling mitigates this: the block scale $s$ shifts the
smallest nonzero value to $0.065 \cdot s$, which can be as small as
${\sim}3.8 \times 10^{-40}$.
The cliff matters only \emph{within} a block where large and near-zero values
coexist --- precisely the pattern produced by ReLU.

\textbf{Implication:} QF8's calibration-free advantage applies to
\emph{weight} quantization.
For W8A8 deployment, activation-aware techniques (e.g., SmoothQuant-style
channel balancing) would likely be needed, partially offsetting the
calibration-free benefit.

\subsection{Quantization-Aware Training}
\label{subsec:qat}

We train GPT-2 Small (162M parameters) from scratch on OpenWebText
(${\sim}9$B tokens) with STE-based quantization-aware training (W8A8: both
weights and activations quantized via block-scaled round-trip at each forward
pass, with straight-through gradient estimation).
Training uses the GPT-2 BPE tokenizer (50{,}257 vocab), sequence length
1{,}024, effective batch size $262$K tokens/step (micro-batch 32, gradient
accumulation 8), learning rate $3 \times 10^{-4}$ with cosine decay, on NVIDIA
H200 GPUs.
FP32 was trained with 3 seeds; QF8 with 1 seed (matched to FP32 seed~42).
Training was terminated by cluster walltime before convergence (val loss still
declining).

\begin{table}[ht]
\centering
\caption{GPT-2 Small QAT on OpenWebText (162M parameters, H200 GPUs).
  FP32: 3 seeds $\times$ 54K steps (${\sim}14$B tokens).
  QF8: 1 seed $\times$ 43K steps (${\sim}11$B tokens).
  Val loss measured on held-out 5K-document split.
  Training was terminated by walltime; val loss was still declining.}
\label{tab:qat}
\begin{tabular}{@{}lccc@{}}
\toprule
Format & Val Loss at 40K steps & $\Delta$ vs FP32 & Steps reached \\
\midrule
FP32 (3 seeds)  & $3.127 \pm 0.009$ & ---         & 54{,}000 \\
QF8 (seed 42)   & $3.131$           & $+0.016\%$  & 43{,}000 \\
\midrule
FP32 seed 42    & $3.131$           & ---         & 54{,}000 \\
QF8 seed 42     & $3.131$           & $+0.016\%$  & 43{,}000 \\
\bottomrule
\end{tabular}
\end{table}

At 40{,}000 matched steps (the last checkpoint before QF8 was terminated),
QF8's validation loss is $3.1310$ vs.\ FP32's $3.1305$ (same seed) ---
a delta of $+0.0005$ ($+0.016\%$).
The three FP32 seeds show cross-seed standard deviation of $0.009$,
confirming that the $+0.016\%$ QF8 gap is well within seed-to-seed noise.
At this point the model has processed ${\sim}11$B tokens and reached validation
loss ${\sim}3.13$ (perplexity ${\sim}23$), substantially beyond the
early-training regime.

\textbf{Limitations.}
QF8 has only 1 seed (no error bars); FP8 was not included in this run.
Training was terminated by cluster walltime at $27\%$ of the target.
The model is not fully converged, so this result demonstrates training
compatibility through ${\sim}11$B tokens, not final converged quality.
Multi-seed QAT to full convergence remains future work
(Section~\ref{sec:future}).

\subsection{Synthetic Benchmarks}
\label{subsec:synthetic}

To confirm the distribution-independence predicted by the equalization property,
we measure SQNR on random matrices and synthetic distributions:

\begin{table}[ht]
\centering
\caption{Matrix multiplication SQNR across sizes (vs.\ float64 ground truth).}
\label{tab:matmul-sqnr}
\begin{tabular}{@{}lcccc@{}}
\toprule
Size & bfloat16 & float32 & FP8 E4M3 (block) & \textbf{QF8} \\
\midrule
$16 \times 32 \times 16$     & 52.3 dB & 139.0 dB & 28.6 dB & \textbf{35.3 dB} \\
$64 \times 128 \times 64$    & 52.5 dB & 133.6 dB & 28.4 dB & \textbf{35.1 dB} \\
$128 \times 256 \times 128$  & 52.6 dB & 130.8 dB & 28.5 dB & \textbf{35.1 dB} \\
\bottomrule
\end{tabular}
\end{table}

\begin{table}[ht]
\centering
\caption{SQNR across value distributions.}
\label{tab:dist-sqnr}
\begin{tabular}{@{}lccc@{}}
\toprule
Distribution & FP8 E4M3 (block) & QF8 & QF8 advantage \\
\midrule
$\mathcal{N}(0, 0.02^2)$ --- weights    & 31.4 dB & 38.2 dB & $+$6.7 dB \\
$\mathcal{N}(0, 1)$ --- post-LN acts    & 31.6 dB & 37.9 dB & $+$6.3 dB \\
$\mathrm{LogN}(0, 1)$ --- gradients     & 31.5 dB & 38.3 dB & $+$6.8 dB \\
$\mathrm{Laplace}(0, 0.02)$ --- sparse  & 31.5 dB & 38.0 dB & $+$6.5 dB \\
90\% sparse $+$ $\mathcal{N}(0,1)$      & 31.7 dB & 38.3 dB & $+$6.6 dB \\
\bottomrule
\end{tabular}
\end{table}

The $+6.6$~dB advantage is consistent across all tested distributions and
matrix sizes, confirming that it is a structural property of the format (the
equalization property) rather than a distribution-specific artifact.


%% ═══════════════════════════════════════════════════════
\section{Future Work}
\label{sec:future}
%% ═══════════════════════════════════════════════════════

\subsection{Remaining Gaps}

Weight-only PTQ results from 124M to 7B parameters
(Section~\ref{subsubsec:scaling-7b}) and the cross-model equalization experiment
(Section~\ref{subsec:equalization-experiment}) validate QF8 for inference-time
weight quantization across diverse architectures and scales.
Several gaps remain:
\begin{enumerate}[leftmargin=2em]
  \item \textbf{W8A8 activation handling.}
    QF8's log-uniform grid is structurally suboptimal for near-zero-concentrated
    activations (Section~\ref{subsec:ptq}).
    Activation-aware calibration (e.g., SmoothQuant-style channel balancing) is
    the most promising mitigation, but would reintroduce per-layer
    calibration --- partially offsetting the format's calibration-free appeal.
  \item \textbf{QAT at scale.}
    The GPT-2 Small experiment (Section~\ref{subsec:qat}) shows near-zero
    degradation but is single-seed.
    Multi-seed QAT at 1B+ scale is needed to establish training robustness.
  \item \textbf{Scaling beyond 7B.}
    Weight-only PTQ at 13B--70B would strengthen the scaling story.
    The equalization property predicts identical SQNR at any scale; confirming
    this for perplexity at 70B would be definitive.
  \item \textbf{RTL synthesis.}
    Hardware gate counts are NAND2-equivalent estimates.
    Actual synthesis at 7nm or 28nm would validate (or correct) the claimed area
    and power advantages.
  \item \textbf{Non-ML domains.}
    The theoretical guarantees apply to any multiply-accumulate workload, but
    empirical validation has been limited to ML weight tensors.
    Evaluation on telecommunications signals, scientific simulation data, and
    edge-computing sensor streams would broaden the evidence base.
\end{enumerate}

\subsection{Gradient Quantization}

QF8's \texttt{u3.4} encoding gives 8~octaves per element, which may be
insufficient for gradients (which need wider dynamic range).
A \texttt{u4.3} variant (4~integer + 3~fractional bits: wider range, lower
precision) for backward passes is a natural extension but has not been
validated.
FP8 training uses E5M2 for gradients for the same reason.

\subsection{BMD Decoder Conjecture}

\begin{conjecture}[BMD Decoder Characterization]
\label{conj:bmd}
The class of monotone BMD decoders from $B$-bit integers to positive reals is
exactly the class of functions expressible as
$\hat{D}(n) = \mathrm{float\_reinterpret}(a \cdot n + b)$ for integer constants
$a, b$, or compositions of a bounded number of such reinterpretations with
integer arithmetic.
\end{conjecture}

This is speculative: we have no proof, no counterexample, and no evidence beyond
the observation that known BMD decoders (LUT, Quake trick, quadratic
approximation) all fit this pattern.
We include it as an open question for the hardware/arithmetic community.


%% ═══════════════════════════════════════════════════════
%% APPENDICES
%% ═══════════════════════════════════════════════════════
\appendix


%% ═══════════════════════════════════════════════════════
\section{Product Error Decomposition}
\label{sec:appendix-product}
%% ═══════════════════════════════════════════════════════

This appendix addresses how quantization error propagates through
multiplication --- the core operation in multiply-accumulate workloads.

\subsection{Auxiliary Quantities}

\begin{definition}[Auxiliary Quantities]
\label{def:auxiliary}
For a random variable $X$ with quantizer $Q$ and error $\delta_X = X - Q(X)$:
\begin{align}
  \alpha_X &= \E[X\delta_X]/\E[X^2]
    & &\text{(signal-error correlation)} \\
  \beta_X &= \E[Q(X)\delta_X]/\E[X^2]
    & &\text{(reconstruction-error correlation)} \\
  \gamma_X &= \E[Q(X)^2]/\E[X^2]
    & &\text{(quantized power ratio)} \\
  \rho_X &= \E[X \cdot Q(X)]/\E[X^2]
    & &\text{(signal-quantized correlation)}
\end{align}
Algebraic identities (no assumptions required):
\begin{equation}
  \alpha_X = \beta_X + \NMSE_X, \qquad
  \gamma_X = 1 - 2\alpha_X + \NMSE_X, \qquad
  \rho_X = 1 - \alpha_X
  \label{eq:algebraic-id}
\end{equation}
\end{definition}

\begin{definition}[Centroid Condition]
\label{def:centroid}
A quantizer satisfies the centroid condition if
$\E[\delta_X \mid X \in S_k] = 0$ for every cell.
This holds for Lloyd-Max quantizers and approximately for any well-designed
quantizer in the high-resolution regime.
Under the centroid condition:
$\beta_X = 0$, $\alpha_X = \NMSE_X$, and $\gamma_X = 1 - \NMSE_X$.
\end{definition}

\subsection{The General Formula}

\begin{theorem}[General Product Error Decomposition]
\label{thm:product-general}
Let $X, Y$ be independent positive random variables with quantizers
$Q_X, Q_Y$.
Then:
\begin{equation}
  \boxed{\NMSE_{\mathrm{prod}} = \NMSE_X + \NMSE_Y + \NMSE_X \cdot \NMSE_Y
  + 2\alpha_X \alpha_Y
  - 2\alpha_X \cdot \NMSE_Y - 2\NMSE_X \cdot \alpha_Y}
  \label{eq:product-general}
\end{equation}
where $\alpha_X = \E[X\delta_X]/\E[X^2]$.
Equivalently:
\begin{equation}
  \NMSE_{\mathrm{prod}} = 1 - 2\rho_X \rho_Y + \gamma_X \gamma_Y
  \label{eq:product-rho-gamma}
\end{equation}
This is exact, using only $X \perp Y$.
No high-resolution, centroid, or distributional assumptions.
\end{theorem}

\begin{proof}
\textbf{Step 1.} Decompose the product error:
\[
  XY - Q_X Q_Y = (Q_X + \delta_X)(Q_Y + \delta_Y) - Q_X Q_Y
  = \underbrace{Q_X \delta_Y}_{A}
  + \underbrace{\delta_X Q_Y}_{B}
  + \underbrace{\delta_X \delta_Y}_{C}
\]

\textbf{Step 2.} Expand $\E[(A+B+C)^2]$ using $X \perp Y$.
Since $Q_X, \delta_X$ depend only on~$X$ and $Q_Y, \delta_Y$ depend only
on~$Y$, every mixed product factors:
\begin{align*}
  \E[(A+B+C)^2]
  &= \E[Q_X^2]\E[\delta_Y^2] + \E[Q_Y^2]\E[\delta_X^2]
     + \E[\delta_X^2]\E[\delta_Y^2] \\
  &\quad + 2\E[Q_X\delta_X]\E[Q_Y\delta_Y]
     + 2\E[Q_X\delta_X]\E[\delta_Y^2]
     + 2\E[\delta_X^2]\E[Q_Y\delta_Y]
\end{align*}

\textbf{Step 3.} Divide by $\E[X^2]\E[Y^2]$ and substitute
$\beta_X = \alpha_X - \NMSE_X$,
$\gamma_X = 1 - 2\alpha_X + \NMSE_X$:
\[
  \NMSE_{\mathrm{prod}} = \gamma_X \NMSE_Y + \gamma_Y \NMSE_X
  + \NMSE_X \NMSE_Y
  + 2\beta_X \beta_Y + 2\beta_X \NMSE_Y + 2\NMSE_X \beta_Y
\]
Expanding $\beta = \alpha - \NMSE$ and simplifying
yields~\eqref{eq:product-general}.
\end{proof}

\subsection{Centroid Simplification}

\begin{corollary}[Product Error Under Centroid Condition]
\label{cor:product-centroid}
If both quantizers satisfy the centroid condition
($\alpha_X = \NMSE_X$, $\alpha_Y = \NMSE_Y$), then:
\begin{equation}
  \boxed{1 - \NMSE_{\mathrm{prod}} = (1 - \NMSE_X)(1 - \NMSE_Y)}
  \label{eq:product-preservation}
\end{equation}
Equivalently:
$\NMSE_{\mathrm{prod}} = \NMSE_X + \NMSE_Y - \NMSE_X \cdot \NMSE_Y$.
\end{corollary}

\begin{proof}
Substitute $\alpha_X = n_X$, $\alpha_Y = n_Y$
into~\eqref{eq:product-general}:
$n_X + n_Y + n_X n_Y + 2n_X n_Y - 2n_X n_Y - 2n_X n_Y
= n_X + n_Y - n_X n_Y$.
\end{proof}

\begin{remark}[Interpretation]
The identity $(1 - n_X)(1 - n_Y) = 1 - n_{\mathrm{prod}}$ means the signal
preservation fraction of the product equals the product of individual
preservation fractions.
Since $n_X n_Y = O(1/N^4)$ while $n_X + n_Y = O(1/N^2)$, the cross-term is
negligible in practice.
Both forms~\eqref{eq:product-general} and~\eqref{eq:product-rho-gamma} have
been confirmed by exhaustive computation over 14{,}641 grid points covering all
combinations of $(\NMSE_X, \NMSE_Y) \in \{0, 0.01, \ldots, 1\}^2$.
\end{remark}

\subsection{Specialization to Log-Uniform Quantizers}

\begin{theorem}[Log-Uniform Product Error]
\label{thm:product-loguniform}
For log-uniform quantizers with geometric midpoints,
$\MSRE = \NMSE = \varepsilon^2/12$ for any density, and:
\begin{equation}
  \NMSE_{\mathrm{prod}} = \frac{(R\ln 2)^2}{6N^2} + O(1/N^4)
  = 2\NMSE^* + O(1/N^4)
\end{equation}
\end{theorem}

\subsection{Optimality for Multiplication}

\begin{theorem}[Log-Uniform Optimal for Multiplication]
\label{thm:product-optimal}
Among all $N$-level centroid-satisfying quantizer pairs $(Q_X, Q_Y)$ on $[a,b]$,
the log-uniform quantizer minimizes the worst-case product NMSE:
\[
  Q_X^* = Q_Y^* = Q_{\log} = \argmin_{Q_X, Q_Y \in \mathcal{Q}_N^c}
  \max_{f_X, f_Y} \NMSE_{\mathrm{prod}}
\]
The minimax value is $2n^* - (n^*)^2$ where $n^* = \varepsilon^2/12$.
\end{theorem}

\begin{proof}
Under the centroid condition,
$\NMSE_{\mathrm{prod}} = n_X + n_Y - n_X n_Y$, and
$\partial/\partial n_X(n_X + n_Y - n_X n_Y) = 1 - n_Y > 0$
(monotonicity).
By Theorem~\ref{thm:minimax}, $Q_{\log}$ achieves $\NMSE = n^*$ for all
densities (density-independence).
For any $Q_X \neq Q_{\log}$, there exists $f_X^*$ with
$\NMSE(Q_X, f_X^*) > n^*$, giving strictly worse product NMSE.
\end{proof}

\begin{remark}[Centroid Restriction]
The centroid condition is without loss of generality: replacing each codepoint
by its conditional mean yields a quantizer with strictly lower NMSE and the same
cell partition.
\end{remark}

\subsection{Extension to Dot Products}

\begin{corollary}[Dimension-Free Dot Product NMSE]
\label{cor:dotproduct}
For $Z = \sum_{i=1}^d w_i x_i$ with independent pairs, iid within type, and
centroid-condition quantizers:
\begin{equation}
  \NMSE_Z \approx \NMSE_w + \NMSE_x + O(1/N^4)
\end{equation}
The NMSE \emph{does not grow with dimension~$d$}.
\end{corollary}

\begin{proof}
Under independence across dimensions:
$\E[(Z - \hat{Z})^2] = d \cdot \E[w^2 x^2](n_w + n_x - n_w n_x)$
and $\E[Z^2] = d \cdot \E[w^2 x^2]$.
Division yields the result.
\end{proof}

\begin{remark}[Independence Assumption]
\label{rem:independence}
The dimension-free result requires independence of the pairs $(w_i, x_i)$ across
dimensions~$i$.
This holds for standard weight--activation products in feedforward layers but
does \emph{not} hold in attention mechanisms, where queries and keys are derived
from the same input.
The dimension-free approximation remains a useful guide even when independence
is approximate.
\end{remark}


%% ═══════════════════════════════════════════════════════
\section{Exact MSRE Derivation}
\label{sec:appendix-msre}
%% ═══════════════════════════════════════════════════════

\begin{theorem}[Exact Per-Cell MSRE]
\label{thm:exact-msre}
For a log-uniform quantizer cell with common ratio $r = 2^{R/N}$ and geometric
midpoint codepoint, the exact MSRE (under uniform intra-cell density) is:
\begin{equation}
  \MSRE_k = 2 - \frac{2\sqrt{r}\,\ln r}{r - 1}
  \label{eq:exact-msre}
\end{equation}
For small $\varepsilon = \ln r = R\ln 2/N$:
\begin{equation}
  \boxed{\MSRE_k = \frac{\varepsilon^2}{12} - \frac{7\varepsilon^4}{2880}
  + O(\varepsilon^6)}
  \label{eq:msre-taylor}
\end{equation}
\end{theorem}

\begin{proof}
For a cell $[a, ra)$ with geometric midpoint $c = a\sqrt{r}$, substituting
$u = x/a$:
\[
  \MSRE_k = \frac{1}{r-1}\int_1^r
  \!\left(1 - \frac{\sqrt{r}}{u}\right)^{\!2} du
  = \frac{2(r-1) - 2\sqrt{r}\ln r}{r - 1}
  = 2 - \frac{2\sqrt{r}\,\ln r}{r - 1}
\]
For the Taylor expansion, substitute $r = e^\varepsilon$:
\[
  \frac{2\sqrt{r}\,\ln r}{r-1}
  = \frac{2\varepsilon\, e^{\varepsilon/2}}{e^\varepsilon - 1}
  = \frac{2(1 + \varepsilon/2 + \varepsilon^2/8 + \cdots)}
         {1 + \varepsilon/2 + \varepsilon^2/6 + \cdots}
  = 2\!\left(1 - \frac{\varepsilon^2}{24} + O(\varepsilon^3)\right)
\]
Therefore
$\MSRE_k = 2 - (2 - \varepsilon^2/12) = \varepsilon^2/12 + O(\varepsilon^4)$.
This confirms the high-resolution approximation.
\end{proof}

\begin{remark}[Midpoint Choice: Geometric vs.\ Harmonic]
\label{rem:midpoint-choice}
The format definition and Theorem~\ref{thm:exact-msre} use geometric midpoint
codepoints $c_k = a r^{k+1/2}$, which minimize per-cell MSRE under uniform
intra-cell density.
An alternative is the harmonic mean
$c_k = 2q_k q_{k+1}/(q_k + q_{k+1})$, which minimizes the worst-case absolute
relative error within each cell to $(r-1)/(r+1)$.
The two midpoints agree to first order:
$c_k^{\mathrm{geo}} = c_k^{\mathrm{harm}}\bigl(1 + O(\varepsilon^2)\bigr)$,
and both yield $\MSRE = \varepsilon^2/12 + O(\varepsilon^4)$.
Crucially, the minimax optimality of log-uniform \emph{spacing}
(Theorem~\ref{thm:minimax}) depends only on cell widths $w(x)/x$, not on the
choice of reconstruction point within each cell.
\end{remark}


%% ═══════════════════════════════════════════════════════
\section{Exponential Separation and Format Comparison}
\label{sec:appendix-separation}
%% ═══════════════════════════════════════════════════════

\subsection{Log vs.\ Uniform: Exponential MSRE Separation}

\begin{theorem}[Exponential Separation Under MSRE]
\label{thm:separation}
For $X \sim \mathrm{LogNormal}(0, \sigma^2)$ quantized to $N$ levels:
\begin{enumerate}[label=(\roman*)]
  \item \textbf{Under MSRE:} the ratio of uniform quantization MSRE to
    logarithmic quantization MSRE is exponential in $\sigma^2$:
    \begin{equation}
      \frac{\MSRE_{\mathrm{uniform}}}{\MSRE_{\mathrm{log}}}
      = e^{\Theta(\sigma^2)}
      \label{eq:msre-separation}
    \end{equation}
  \item \textbf{Under MSE:} the two quantizers achieve comparable distortion
    (ratio $\approx 1$).
\end{enumerate}
\end{theorem}

This result is significant because MSRE is the natural metric for
floating-point systems --- it measures relative accuracy, which determines
signal quality.

\begin{table}[ht]
\centering
\caption{MSRE and MSE ratios (uniform / log) for LogNormal($0, \sigma^2$),
$N = 256$.}
\label{tab:separation}
\begin{tabular}{@{}ccccc@{}}
\toprule
$\sigma$ & MSE ratio & MSE dB & MSRE ratio & MSRE dB \\
\midrule
1.00  & 0.98  & $-0.1$ & 8.5        & 9.3  \\
1.50  & 0.99  & $-0.0$ & 1,520      & 31.8 \\
2.00  & 0.99  & $-0.0$ & 346,000    & 55.4 \\
2.77  & 1.00  & $0.0$  & $3.0 \times 10^9$ & 94.8 \\
\bottomrule
\end{tabular}
\end{table}

\begin{proof}[Proof sketch]
For $X = e^Z$ with $Z \sim \mathcal{N}(0, \sigma^2)$:
Log quantization on $X$ corresponds to uniform quantization on $Z$ (Gaussian),
giving $\MSRE_{\log} \approx \varepsilon^2/12$.
Uniform quantization on $X$ translates to highly nonuniform quantization on
$Z = \ln X$: at $Z = k\sigma$, the local step in log-space grows as
$\propto e^{2k\sigma}$, and integrating against the Gaussian density yields
$\MSRE_{\mathrm{uniform}} \propto e^{c\sigma^2}$.
MSE is insensitive because it is dominated by absolute error on large values
where both quantizers are comparable.
\end{proof}

\begin{remark}[Scope]
\label{rem:separation-scope}
Theorem~\ref{thm:separation} compares log-uniform quantization against
\emph{truly uniform} (fixed-step) quantization --- the regime of INT8.
FP8 E4M3 is \emph{not} a uniform quantizer: within each binade it has 8
uniformly-spaced levels, but across binades the step size doubles, making it
quasi-logarithmic at coarse scale.
The exponential MSRE separation applies to the QF8-vs-INT8 comparison, not to
the QF8-vs-FP8 comparison.
\end{remark}

\subsection{QF8 vs.\ FP8 E4M3: Levels-per-Octave Advantage}
\label{subsec:qf8-vs-fp8}

The $+6.6$~dB advantage of QF8 over FP8 E4M3 has a simpler origin than the
exponential separation above: QF8 provides 16~levels per octave vs.\ FP8's~8,
giving a step-size ratio of~$2$ and an MSRE ratio of~$4$
($10\log_{10}(4) = 6.0$~dB).
The remaining ${\sim}0.6$~dB arises from FP8's piecewise-uniform spacing within
each binade:

\begin{table}[ht]
\centering
\caption{Within-binade precision comparison.}
\label{tab:qf8-vs-fp8-binade}
\begin{tabular}{@{}lcccc@{}}
\toprule
Metric & IEEE E4M3 & Log-uniform (8/oct) & Ratio & dB gap \\
\midrule
Average MSRE (per binade) & $6.510 \times 10^{-4}$ & $6.256 \times 10^{-4}$
  & 1.041 & 0.2 \\
Peak relative error & 0.0625 & 0.04427 & 1.41 & --- \\
Worst-case MSRE (HR) & $1.302 \times 10^{-3}$ & $6.256 \times 10^{-4}$
  & 2.08 & 3.18 \\
\bottomrule
\end{tabular}
\end{table}

\begin{remark}
The FP8 E4M3 SQNR of 31.5~dB is verified by per-binade analysis across all
126 positive representable values, correcting an earlier figure of 25.1~dB that
arose from an error in the dynamic-range assumption.
\end{remark}

\subsection{Comparison with Johnson's ELMA}
\label{subsec:vs-johnson}

\begin{table}[ht]
\centering
\caption{QF8 vs.\ Johnson's ELMA.}
\label{tab:vs-johnson}
\begin{tabular}{@{}p{3.5cm}p{5cm}p{5cm}@{}}
\toprule
Feature & Johnson ELMA (8-bit) & QF8 \\
\midrule
Log fractional bits  & $\sim$6 (posit tapered)     & 4 (fixed \texttt{u3.4}) \\
Encoding             & Posit regime bits (variable) & Fixed-width \texttt{u3.4} \\
Block scaling        & None (self-scaled)           & E8M0 per 32 elements \\
LUT size             & 64 entries ($2^6$)
  & 16 entries ($2^4$) --- $4\times$ smaller \\
Optimality proof     & None
  & Minimax NMSE (Theorem~\ref{thm:minimax}) \\
\bottomrule
\end{tabular}
\end{table}


%% ═══════════════════════════════════════════════════════
\section{Rate-Distortion Context}
\label{sec:appendix-rd}
%% ═══════════════════════════════════════════════════════

\subsection{Gaussian Source Rate-Distortion}

For $X \sim \mathcal{N}(0, \sigma^2)$:
\[
  R(D) = \frac{1}{2}\log_2\!\left(\frac{\sigma^2}{D}\right),
  \quad D \leq \sigma^2
\]
This yields the \textbf{6 dB/bit rule}: each additional bit reduces MSE by
$4\times$ (6.02 dB).

\subsection{Scalar Quantization Gap (Zador)}

The maximum improvement from scalar to infinite-dimensional vector
quantization:
\[
  \frac{G_1}{G_\infty} = \frac{\pi e}{6} \approx 1.42
  \quad (1.53 \text{ dB})
\]
This modest gap means a well-designed scalar quantizer like QF8 comes within
1.53~dB of the VQ optimum.

\subsection{Bit Allocation}

For tensor types $t \in \{w, a, g\}$ with sensitivity weights $c_t$ and
distribution variances $\sigma_t^2$, the optimal allocation minimizes:
$\sum_t c_t \sigma_t^2 \cdot 2^{-2B_t}$ subject to
$\sum_t B_t = B_{\text{total}}$.

\textbf{Solution (reverse water-filling):}
\[
  B_t^* = \frac{B_{\text{total}}}{|\mathcal{T}|}
  + \frac{1}{2}\log_2\!\left(\frac{c_t \sigma_t^2}{G}\right),
  \qquad G = \left(\prod_t c_t \sigma_t^2\right)^{1/|\mathcal{T}|}
\]

With corrected parameters ($c_w = 1, c_a = 1, c_g = 10^{-6}$), the near-zero
gradient allocation ($B_g = 0.18$) reflects that
$c_g = \eta^2 \approx 10^{-6}$ dramatically reduces gradient sensitivity,
motivating stochastic rounding or shared-exponent compression for gradients.


%% ═══════════════════════════════════════════════════════
\section*{References}
%% ═══════════════════════════════════════════════════════

\begin{thebibliography}{19}
  \bibitem{micikevicius2018} Micikevicius, P.\ et al.\ (2018).
    ``Mixed Precision Training.''
    \textit{ICLR 2018}. arXiv:1710.03740.
  \bibitem{micikevicius2022} Micikevicius, P.\ et al.\ (2022).
    ``FP8 Formats for Deep Learning.''
    arXiv:2209.05433.
  \bibitem{rouhani2023} Rouhani, B.\ et al.\ (2023).
    ``Microscaling Data Formats for Deep Learning.''
    arXiv:2310.10537.
  \bibitem{smith1957} Smith, B.\ (1957).
    ``Instantaneous Companding of Quantized Signals.''
    \textit{Bell System Technical Journal} 36(3):653--709.
  \bibitem{horowitz2014} Horowitz, M.\ (2014).
    ``Computing's Energy Problem (and what we can do about it).''
    \textit{ISSCC}.
  \bibitem{johnson2018} Johnson, J.\ (2018).
    ``Rethinking Floating Point for Deep Learning.''
    arXiv:1811.01721.
  \bibitem{gustafson2017} Gustafson, J.\ and Yonemoto, I.\ (2017).
    ``Beating Floating Point at its Own Game: Posit Arithmetic.''
    \textit{Supercomputing Frontiers and Innovations} 4(2):71--86.
  \bibitem{miyashita2016} Miyashita, D.\ et al.\ (2016).
    ``Convolutional Neural Networks using Logarithmic Data Representation.''
    arXiv:1603.01025.
  \bibitem{kingsbury1971} Kingsbury, N.\ and Rayner, P.\ (1971).
    ``Digital filtering using logarithmic arithmetic.''
    \textit{Electronics Letters}.
  \bibitem{swartzlander1975} Swartzlander, E.\ and Alexopoulos, A.\ (1975).
    ``The Sign/Logarithm Number System.''
    \textit{IEEE Trans.\ Computers}.
  \bibitem{dettmers2023} Dettmers, T.\ et al.\ (2023).
    ``QLoRA: Efficient Finetuning of Quantized LLMs.''
    arXiv:2305.14314.
  \bibitem{bennett1948} Bennett, W.R.\ (1948).
    ``Spectra of Quantized Signals.''
    \textit{Bell System Technical Journal} 27(3):446--472.
  \bibitem{panter1951} Panter, P.F.\ and Dite, W.\ (1951).
    ``Quantization Distortion in Pulse-Count Modulation with Nonuniform
    Spacing of Levels.''
    \textit{Proceedings of the IRE} 39(1):44--48.
  \bibitem{sun2011} Sun, J.Z.\ and Goyal, V.K.\ (2011).
    ``Scalar Quantization for Relative Error.''
    \textit{Proc.\ IEEE Data Compression Conference (DCC)}, pp.~293--302.
  \bibitem{gray1998} Gray, R.M.\ and Neuhoff, D.L.\ (1998).
    ``Quantization.''
    \textit{IEEE Trans.\ Information Theory} 44(6):2325--2383.
  \bibitem{gersho1992} Gersho, A.\ and Gray, R.M.\ (1992).
    \textit{Vector Quantization and Signal Compression}.
    Kluwer Academic.
  \bibitem{jouppi2017} Jouppi, N.\ et al.\ (2017).
    ``In-Datacenter Performance Analysis of a Tensor Processing Unit.''
    \textit{ISCA}.
  \bibitem{sze2017} Sze, V.\ et al.\ (2017).
    ``Efficient Processing of Deep Neural Networks: A Tutorial and Survey.''
    \textit{Proc.\ IEEE}.
  \bibitem{kuzmin2022} Kuzmin, A.\ et al.\ (2022).
    ``FP8 Quantization: The Power of the Exponent.''
    arXiv:2208.09225.
\end{thebibliography}

\end{document}
